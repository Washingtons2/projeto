defmodule ProjetoWeb.MessagesController do
  use ProjetoWeb, :controller

  @doc """
  ProjetoWeb.MessagesController.index
  """

  def index(conn, _params) do
    json(conn, %{"apl" => "ddsdsd"})
  end
end
